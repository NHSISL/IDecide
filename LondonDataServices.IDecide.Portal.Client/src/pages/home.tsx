import React from "react";
import { Container, Row, Col } from "react-bootstrap";
import { Card, Checkboxes, Radios } from "nhsuk-react-components";

export const Home = () => {
    return (
        <Container className="mt-4">
            <Row></Row>
        </Container>
    );
};